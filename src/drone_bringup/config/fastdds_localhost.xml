<?xml version="1.0" encoding="UTF-8"?>
<!--
fastdds_localhost.xml
Restricts FastDDS (ROS2 RMW) to loopback/127.0.0.1 only.
This prevents "Network is unreachable" multicast errors when the
host's loopback interface has no MULTICAST flag.

Usage: set FASTRTPS_DEFAULT_PROFILES_FILE=/path/to/this/file
       before launching any ROS2 process.
-->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <transport_descriptors>
    <transport_descriptor>
      <transport_id>localhost_udp</transport_id>
      <type>UDPv4</type>
      <interfaceWhiteList>
        <address>127.0.0.1</address>
      </interfaceWhiteList>
    </transport_descriptor>
  </transport_descriptors>

  <participant profile_name="participant_profile" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>localhost_udp</transport_id>
      </userTransports>
      <useBuiltinTransports>false</useBuiltinTransports>
    </rtps>
  </participant>
</profiles>
